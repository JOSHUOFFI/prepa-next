# PrePa Next.js Foundation

This standalone Next.js App Router project is the Phase 2 migration foundation. The legacy application remains untouched in `../PrePa`.

Run `npm install`, then `npm run dev` from this directory. Authentication, the CBT engine, persistence, administration, and AI Classroom are intentionally deferred.
